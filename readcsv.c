// D:/github/VirtualHeart&Pacemaker/cases/EP_AVNRT/egm_table.csv
// C program for the above approach
#include <conio.h>
#include <stdio.h>
#include <string.h>

// Driver Code
int main()
{
    // Substitute the full file path
    // for the string file_path
    FILE *fp = fopen("D:/github/VirtualHeart&Pacemaker/cases/EP_AVNRT/egm_table.csv", "r");

    if (!fp)
        printf("Can't open file\n");

    else
    {
        // all of data is save between "" use this to get data
    }
